@model IEnumerable<WebASPCorePremiereApp.Models.Livre>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Nos Livres</h1>

<p>
    <a asp-action="Create">Ajouter un livre</a>
</p>
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Annee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Resume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Photo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ISBN)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Theme)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>*@
@*@foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Titre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Annee)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Resume)
            </td>
            <td>
                <img alt="Photo du livre" src="~/Images/@Html.DisplayFor(modelItem => item.Photo)" width="80" height="120" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ISBN)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Theme.Libelle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-success">Modifier</a>
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Afficher</a>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Supprimer</a>
            </td>
        </tr>*@


<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-3">
            <div class="card" style="border: 2px solid #B6E28F; margin-bottom: 10px;">
                @*<div class="card-header"> @Html.DisplayNameFor(model => model.Theme): @Html.DisplayFor(modelItem => item.Theme) </div>*@
                <div class="card-body" style="padding:3px">
                    <img alt="photo livre" style="padding:2px;width:100%; height:16rem" class="img-fluid" src="~/Images/@Html.DisplayFor(modelItem => item.Photo)" />

                    <h5 class="card-title" style="font:bold"> @Html.DisplayFor(modelItem => item.Titre) </h5>
                    <h5 class="card-title">  @Html.DisplayFor(modelItem => item.Annee) </h5>
                    <h5 class="card-title">  @Html.DisplayFor(modelItem => item.Theme.Libelle) </h5>
                    <h5 class="card-title">  @Html.DisplayFor(modelItem => item.Resume) </h5>
                    <div>
                        @Html.ActionLink("Modifier", "Edit", new { id = item.Id }, new { @class = "btn btn-success" })
                        @Html.ActionLink("Afficher", "Details", new { id = item.Id }, new { @class = "btn btn-info" })
                        @Html.ActionLink("Supprimer", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
                    </div>
                    @*<a href="#" class="btn btn-primary">Go somewhere</a>*@
                </div>
            </div>
        </div>
    }
</div>
@*</tbody>
    </table>*@
